<div class="container my-2 ">

<style>

.card {
  box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
  max-width: 300px;
  margin: auto;
  text-align: center;
}

.title {
  color: grey;
  font-size: 18px;
}

button {
  border: none;
  outline: 0;
  display: inline-block;
  padding: 8px;
  color: white;
  background-color: #000;
  text-align: center;
  cursor: pointer;
  width: 100%;
  font-size: 18px;
}

a {
  text-decoration: none;
  font-size: 22px;
  color: black;
}

button:hover, a:hover {
  opacity: 0.7;
}</style>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">

<div class="card">
  <img src="<%= current_user.avatar %>" alt="John" style="width:100%">
  
  <p class="title"><%= current_user.email %></p>
  <p class="title"><%= current_user.first_name %> <%= current_user.last_name %></p>
  <%= link_to "Edit Profile",edit_user_registration_path(current_user), class: "btn btn-primary my-1"   %>
  <%= link_to "Delete Profile",user_registration_path(current_user),method: :delete, data: {  confirm: 'Are you sure?' }, class: "btn btn-danger my-1"   %>

</div>

</div>